import React, { type ReactElement } from 'react';

export function App(): ReactElement {
	return (
		<div className="bg-gray-800 w-full h-svh text-red-50">
			<span>Hello there friendo!</span>
		</div>
	);
}
